#include <cstddef>

void Adam(const size_t & max_iteration, const size_t & batch_size, const double & learning_rate);

void SGD(const size_t & max_iteration, const size_t & batch_size, const double & learning_rate);

void trust_region(const size_t & max_iteration);